import 'package:flutter/material.dart';

class ChatTbGroup extends StatelessWidget {
  const ChatTbGroup({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("page 1"),
    );
  }
}
